<h2>Edit <%= @page.address %></h2>
<p><%= link_to "Return to directory listing", page_category_pages_path(params[:page_category_id])%></p>
<% form_for (@page, :url => page_category_page_path(params[:page_category_id], @page)) do |f| %>
<%= render :partial => f %>
<% end %>

